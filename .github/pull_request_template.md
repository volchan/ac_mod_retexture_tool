## What

<!-- One sentence: what does this PR do? -->

## Why

<!-- Why is this change needed? What problem does it solve? -->

## Changes

<!-- Bullet list of notable changes -->

-

## Checklist

- [ ] `task lint` passes with no errors
- [ ] `task test` passes with no failures
- [ ] `task test:coverage` run for any new logic
- [ ] Visual changes verified with `task dev`
- [ ] No `unwrap()` calls added in Rust (use `?` and proper error types)

## Screenshots

<!-- If UI changed, add before/after screenshots. Otherwise delete this section. -->
